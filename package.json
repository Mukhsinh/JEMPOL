{
  "name": "complaint-management-system",
  "version": "1.0.0",
  "description": "Sistem Manajemen Keluhan dengan Supabase Backend",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "build:skip-check": "npm run build:skip-check --workspace=frontend",
    "vercel-build": "cd frontend && npm install && npm run build:skip-check",
    "test": "npm run test --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "@vercel/node": "^3.0.12"
  }
}
